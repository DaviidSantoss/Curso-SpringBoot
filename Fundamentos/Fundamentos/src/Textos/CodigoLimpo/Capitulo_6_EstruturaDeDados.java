package Textos.CodigoLimpo;

public class Capitulo_6_EstruturaDeDados {

    // =======================
    // Estrutura de Dados
    // =======================

    /* "O código procedural (usado em estrutura de dados) facilita a adição
     *  de novas funções sem precisar alterar as estruturas de dados existentes.
     *  O código orientado a objeto (OO), por outro lado, facilita a adição de novas
     *  classes sem precisar alterar as funções existentes."
     *
     *  Procedural: "Faça isso, depois aquilo, depois aquilo outro."
     *  POO: "Crie objetos que sabem fazer as coisas."
     *
     *
     *  // =======================
     *  // Conclusão
     *  // =======================
     *
     *  Os objetos expõem as ações e ocultam os dados. Isso facilita a adição de novos
     *  tipos de objetos sem precisar modificar as ações existentes e dificulta a inclusão
     *  de novas atividades em objetos existentes. As estruturas de dados expõem os dados e
     *  não possuem ações significativas. Isso facilita a adição de novas ações às estruturas
     *  de dados existentes e dificulta a inclusão de novas estruturas de dados em funções
     *  existentes.
     *
     */
}